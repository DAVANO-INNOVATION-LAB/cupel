---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: artifactscanpolicies.security.davano.io
spec:
  group: security.davano.io
  names:
    kind: ArtifactScanPolicy
    listKind: ArtifactScanPolicyList
    plural: artifactscanpolicies
    singular: artifactscanpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.enforcement
      name: Enforcement
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ArtifactScanPolicy configures scanning and gating for model artifacts.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ArtifactScanPolicySpec defines scanners to run and rules
              to enforce.
            properties:
              enforcement:
                default: Enforce
                description: |-
                  Enforcement controls admission behavior for artifacts failing this
                  policy: Enforce rejects, Warn admits with a warning, Audit only logs.
                enum:
                - Enforce
                - Warn
                - Audit
                type: string
              rules:
                description: PolicyRules are the pass/fail gates evaluated after scanning.
                properties:
                  allowedFormats:
                    description: AllowedFormats restricts model formats (e.g. safetensors,
                      onnx, gguf).
                    items:
                      type: string
                    type: array
                  blockMalware:
                    description: BlockMalware quarantines on any malware finding.
                      Defaults to true.
                    type: boolean
                  blockModelDrift:
                    description: |-
                      BlockModelDrift quarantines when the model's own declarations disagree
                      with the weights they describe at High severity or above — a config
                      advertising a parameter count, precision or architecture the tensors do
                      not implement. Defaults to false: drift is frequently benign (a
                      quantized re-upload carrying the original config is the common case),
                      so it is surfaced by default and gated on request.
                    type: boolean
                  blockSecrets:
                    description: BlockSecrets quarantines on any leaked-secret finding.
                      Defaults to true.
                    type: boolean
                  blockUnsafeModel:
                    description: |-
                      BlockUnsafeModel quarantines on a critical model-inspection finding —
                      a pickle that imports os.system, an archive that escapes its directory,
                      trust_remote_code. These execute on load, so they are as serious as
                      malware. Defaults to true.
                    type: boolean
                  blockedFormats:
                    description: BlockedFormats quarantines specific formats (e.g.
                      pickle).
                    items:
                      type: string
                    type: array
                  maxCriticalCVEs:
                    description: MaxCriticalCVEs above which the artifact is quarantined.
                      Nil = no limit.
                    format: int32
                    type: integer
                  maxHighCVEs:
                    format: int32
                    type: integer
                  requireAIBOM:
                    description: |-
                      RequireAIBOM demands a generated AI bill of materials — a description
                      of the model itself — before approval. This is what the EU AI Act
                      Annex XII, the CISA/G7 minimum elements and Korea's Framework Act each
                      ask a provider to be able to produce, and it is satisfied by a
                      different scanner from RequireSBOM.
                    type: boolean
                  requireProvenance:
                    description: RequireProvenance demands verified provenance attestations.
                    type: boolean
                  requireSBOM:
                    description: RequireSBOM demands a generated SBOM before approval.
                    type: boolean
                  requireSignature:
                    description: RequireSignature demands a verified Cosign signature
                      from a TrustedPublisher.
                    type: boolean
                type: object
              scanners:
                items:
                  description: ScannerSpec configures one scanner in a policy.
                  properties:
                    args:
                      description: Args overrides the scanner container arguments.
                      items:
                        type: string
                      type: array
                    enabled:
                      type: boolean
                    image:
                      description: Image overrides the built-in image for this scanner.
                      type: string
                    name:
                      description: Name of the scanner (clamav, model-inspector, trivy,
                        syft, trufflehog, ...).
                      type: string
                    resources:
                      description: ResourceRequirements describes the compute resource
                        requirements.
                      properties:
                        claims:
                          description: |-
                            Claims lists the names of resources, defined in spec.resourceClaims,
                            that are used by this container.

                            This is an alpha field and requires enabling the
                            DynamicResourceAllocation feature gate.

                            This field is immutable. It can only be set for containers.
                          items:
                            description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                            properties:
                              name:
                                description: |-
                                  Name must match the name of one entry in pod.spec.resourceClaims of
                                  the Pod where this field is used. It makes that resource available
                                  inside a container.
                                type: string
                              request:
                                description: |-
                                  Request is the name chosen for a request in the referenced claim.
                                  If empty, everything from the claim is made available, otherwise
                                  only the result of this request.
                                type: string
                            required:
                            - name
                            type: object
                          type: array
                          x-kubernetes-list-map-keys:
                          - name
                          x-kubernetes-list-type: map
                        limits:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: |-
                            Limits describes the maximum amount of compute resources allowed.
                            More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                          type: object
                        requests:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: |-
                            Requests describes the minimum amount of compute resources required.
                            If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                            otherwise to an implementation-defined value. Requests cannot exceed Limits.
                            More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                          type: object
                      type: object
                    timeoutSeconds:
                      description: TimeoutSeconds bounds the scan job. Defaults to
                        1800.
                      format: int64
                      type: integer
                  required:
                  - name
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
